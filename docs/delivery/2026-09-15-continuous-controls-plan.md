# Continuous controls implementation plan

**Scope:** [FEAT-006 / 0004](../product/specifications/0004-continuous-controls.md), authorized by the maintainer's second hands-on review on 2026-09-15.

1. Preserve the previous complete worktree as a private local checkpoint (completed; excluded from the sanitized public history).
2. Add continuous texture with compatible snapshot validation; build compact preset rails and grouped environment controls, direct/remembered location, slower shared-egg motion and stopped result state.
3. Verify meaningful boundaries and browser journeys, inspect phone output, update living docs and record evidence. Keep actual-device and kitchen acceptance separate.

Use existing dependencies and pure TypeScript science. The consequential extension is an explicit provisional texture multiplier; old named snapshots keep their results. Browser permission is inspected, never silently requested on startup when it would prompt.
