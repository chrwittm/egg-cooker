# MVP implementation plan — 2026-09-15

The maintainer's instruction to build the behavior in `0001-mvp.md` accepts the specification and authorizes local implementation. Existing uncommitted documentation is preserved. Publication is outside this historical delivery slice.

1. Implement the traceable pressure/heating model, input validation, absolute and virtual clocks, illustration and two-screen interface.
2. Add consented environmental lookup with fallbacks, strict session recovery, bounded audio and accessible interactions.
3. Exercise domain/failure paths and production browsers including `/egg-cooker/`; update living docs and record evidence. Actual-device, human visual and kitchen acceptance remain separate release gates.

No new runtime dependencies. Domain arithmetic and transitions stay independent of browser APIs. Browser adapters own external effects; the UI presents their outcomes.
